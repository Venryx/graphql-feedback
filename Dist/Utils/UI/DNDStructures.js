import { CE } from "js-vextensions";
export class DroppableInfo {
    constructor(data) {
        CE(this).Extend(data);
    }
}
export class DraggableInfo {
    constructor(data) {
        CE(this).Extend(data);
    }
}
